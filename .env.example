# Database
DATABASE_URL=postgres://salesintel:password@localhost:5432/salesintel?sslmode=disable

# JWT
JWT_SECRET=your-secret-key-change-in-production

# Default password for new users
DEFAULT_PASSWORD=changeme123

# Optional: Default user ID for legacy fallback
DEFAULT_USER_ID=

# Server
PORT=3000
ENVIRONMENT=development
CORS_ORIGIN=*
